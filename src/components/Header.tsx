const Header = () => {
  return (
    <div>
      <header className="bg-gray-800 text-white p-4 justify-between">
        <h1>Platform Launch</h1>
        <button>Add Task</button>
      </header>
    </div>
  );
};

export default Header;
